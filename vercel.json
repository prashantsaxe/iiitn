{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "rm -f package-lock.json && npm install --no-package-lock",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "cleanUrls": true,
  "github": {
    "silent": true
  }
}
